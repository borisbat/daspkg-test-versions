options gen2

require daslib/daspkg

[export]
def package() {
    package_name("daspkg-test-versions")
    package_author("borisbat")
    package_description("Test package: module with version tags")
    package_tag("test")
}

[export]
def resolve(sdk_version, version : string) {
    if (version == "" || version == "latest") {
        download_tag("v2.0")
    } else {
        download_tag("v{version}")
    }
}
